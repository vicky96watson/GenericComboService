# Application name
spring.application.name=GenericComboServices

# Datasource configuration
spring.datasource.url=jdbc:postgresql://10.10.11.57:5444/uat_mp
spring.datasource.username=uat_mp
spring.datasource.password=uatmp321
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Optional: format SQL for better readability in console
spring.jpa.properties.hibernate.format_sql=true

# Optional: Enable SQL comments (requires Hibernate 5.2+)
spring.jpa.properties.hibernate.use_sql_comments=true
